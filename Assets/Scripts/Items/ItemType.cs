public enum ItemType
{
    None,
    Gun,        // Tabanca (Level 1, 6)
    Axe,        // Balta (Level 2)
    Screwdriver,// Tornavida (Level 3)
    Poison,     // Zehir (Level 4)
    Push        // Ýtme (Level 5 - item yok, sadece etkileþim)
}